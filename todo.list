- service redirection : remember port in addtion of selectors
- logging: (together)
  - add structured logs for mamanger
  - remove rockets fancy logs + structured logs
  - makes logs go to files
  - ks tools to retreive log (nicely) for manager (using logs files)
  - ks tools to retreive log (nicely) for server  (using logs files)
- rust doc
- schema (as code ?)
- create a user-doc (mkdocs ?)